{
  "name": "web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "mkdir -p public && cp -r index.html js public/",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "@vercel/node": "^2.15.1"
  },
  "devDependencies": {
    "vercel": "^32.3.0"
  }
}
